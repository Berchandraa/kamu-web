<div class="floating-actions">
  <a
    class="floating-whatsapp"
    href="https://wa.me/6282338888971"
    target="_blank"
    rel="noopener"
    aria-label="Chat on WhatsApp"
  >
    <svg class="floating-wa-icon" viewBox="0 0 32 32" aria-hidden="true">
      <path d="M16.1 5.1a10.7 10.7 0 0 0-9.2 16.2l-1.2 4.4 4.5-1.2a10.7 10.7 0 1 0 5.9-19.4Zm0 19.5c-1.8 0-3.5-.5-5-1.5l-.4-.2-2.7.7.7-2.6-.3-.4a8.7 8.7 0 1 1 7.7 4Z" />
      <path d="M21.1 18.5c-.3-.1-1.6-.8-1.8-.9-.2-.1-.4-.1-.6.1-.2.3-.7.9-.9 1-.2.2-.3.2-.6.1-.3-.1-1.2-.4-2.3-1.4-.9-.8-1.4-1.7-1.6-2-.2-.3 0-.4.1-.6.1-.1.3-.3.4-.5.1-.2.2-.3.3-.5.1-.2.1-.4 0-.5l-.8-1.9c-.2-.5-.4-.4-.6-.4h-.5c-.2 0-.5.1-.8.4-.3.3-1 1-1 2.4s1 2.8 1.2 3c.1.2 2 3.1 4.9 4.3 1.8.8 2.5.9 3.3.8.5-.1 1.6-.7 1.9-1.3.2-.6.2-1.2.2-1.3-.1-.2-.3-.3-.6-.4Z" />
    </svg>
  </a>
</div>

<style>
  .floating-actions {
    position: fixed;
    right: clamp(1rem, 2vw, 1.75rem);
    bottom: clamp(1rem, 2vw, 1.75rem);
    z-index: 89;
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
  }

  .floating-whatsapp {
    position: relative;
    display: grid;
    width: clamp(3.25rem, 5vw, 4rem);
    aspect-ratio: 1;
    place-items: center;
    border: 1px solid color-mix(in srgb, var(--ink) 18%, transparent);
    border-radius: 999px;
    background: color-mix(in srgb, var(--bg) 88%, transparent);
    color: var(--ink);
    box-shadow: 0 18px 45px rgb(32 32 32 / 0.14);
    backdrop-filter: blur(14px);
    transition:
      transform var(--dur-base) var(--ease-out),
      background-color var(--dur-base) var(--ease-out),
      color var(--dur-base) var(--ease-out);
    border-color: color-mix(in srgb, #25d366 78%, var(--ink));
    background: #25d366;
    color: white;
  }

  .floating-whatsapp:hover {
    transform: translateY(-0.2rem);
    background: #1ebe57;
    color: white;
  }

  .floating-wa-icon {
    width: 1.7rem;
    height: 1.7rem;
    fill: currentColor;
  }
</style>
